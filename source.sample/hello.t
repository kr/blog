This is a plain text file (file extension ".t").

It gets rendered as text and sent as <b>text/plain</b> (so proper
browsers didn't just make that bold).

It is served without the file extension (at /hello).
